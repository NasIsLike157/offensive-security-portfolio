
This is a simple Caesar cipher implementation written in Python. It supports both encryption and decryption using a configurable shift value.

The script processes input text character-by-character, applying the shift only to alphabetical characters while preserving other characters as-is.

It was created as a small exercise to reinforce basic concepts such as character encoding, modular arithmetic, and command-line argument handling.